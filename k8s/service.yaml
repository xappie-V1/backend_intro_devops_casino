apiVersion: v1
kind: Service
metadata:
  name: casino-backend
  labels:
    app: casino-backend
spec:
  # ClusterIP: http://casino-backend:3000
  type: ClusterIP
  selector:
    app: casino-backend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
